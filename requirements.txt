requests>=2.34.2
defusedxml>=0.7

# optional: only for `backtest --price-source yfinance`, which the README
# actively discourages (drops delisted tickers -> survivorship-biased results).
# yfinance>=0.2
